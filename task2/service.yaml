apiVersion: v1
kind: Service
metadata:
  name: insuretech-app-service
  labels:
    app: insuretech-app
spec:
  type: LoadBalancer
  selector:
    app: insuretech-app
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    name: http

